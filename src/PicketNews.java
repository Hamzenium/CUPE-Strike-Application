import java.time.LocalDateTime;

public class PicketNews extends News{

	public PicketNews(String source, String category, String message, int length, LocalDateTime time) {
		super(source, category, message, length, time);
	}

	
}
