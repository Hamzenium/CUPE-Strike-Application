import java.time.LocalDateTime;

public class StudentNews extends News {

	public StudentNews(String source, String category, String message, int length, LocalDateTime time) {
		super(source, category, message, length, time);
	}

}
